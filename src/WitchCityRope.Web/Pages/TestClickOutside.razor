@page "/test-click-outside"

<h3>Test Click Outside Detection</h3>

<div class="test-container">
    <p>This page tests the click-outside detection functionality for the user menu dropdown.</p>
    
    <div class="test-instructions">
        <h4>Test Instructions:</h4>
        <ol>
            <li>Click the "User Menu" button in the header to open the dropdown</li>
            <li>Click anywhere outside the dropdown (but not on the button)</li>
            <li>The dropdown should close automatically</li>
            <li>Click the button again to reopen</li>
            <li>Click on any item inside the dropdown - it should stay open</li>
            <li>Click the button while the dropdown is open - it should toggle closed</li>
        </ol>
    </div>
    
    <div class="test-area">
        <p>Click in this area to test click-outside detection:</p>
        <div class="click-zone">
            <p>Clicking here should close the user menu dropdown if it's open.</p>
        </div>
    </div>
</div>

<style>
    .test-container {
        padding: 20px;
        max-width: 800px;
        margin: 0 auto;
    }
    
    .test-instructions {
        background: #f5f5f5;
        padding: 20px;
        border-radius: 8px;
        margin: 20px 0;
    }
    
    .test-instructions h4 {
        margin-top: 0;
    }
    
    .test-instructions ol {
        margin: 10px 0;
        padding-left: 20px;
    }
    
    .test-instructions li {
        margin: 5px 0;
    }
    
    .test-area {
        margin-top: 30px;
        padding: 20px;
        background: #e8f4f8;
        border: 2px dashed #3498db;
        border-radius: 8px;
        text-align: center;
    }
    
    .click-zone {
        min-height: 200px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: white;
        border-radius: 4px;
        margin-top: 10px;
        cursor: pointer;
        transition: background-color 0.3s;
    }
    
    .click-zone:hover {
        background-color: #f0f0f0;
    }
</style>

@code {
    // This page doesn't need any code - it's just for testing the MainLayout functionality
}