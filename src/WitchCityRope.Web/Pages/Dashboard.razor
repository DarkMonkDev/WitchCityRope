@page "/dashboard"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirect to the member dashboard
        Navigation.NavigateTo("/member/dashboard", true);
    }
}