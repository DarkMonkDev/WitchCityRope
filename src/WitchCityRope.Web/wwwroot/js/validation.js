// Custom validation scripts for WitchCityRope
// This file is intentionally minimal as most validation is handled by jQuery Validation

// Wait for jQuery to be available
if (typeof jQuery === 'undefined') {
    console.warn('validation.js: jQuery not loaded yet');
}

// Initialize any custom validation rules when document is ready
document.addEventListener('DOMContentLoaded', function() {
    // Custom validation logic can go here
    console.log('WitchCityRope validation.js loaded');
});