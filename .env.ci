# CI/CD Environment Configuration
# This file contains default environment variables for CI/CD pipelines

# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=WitchCity2024!
POSTGRES_DB=witchcityrope_test
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# ASP.NET Core Configuration
ASPNETCORE_ENVIRONMENT=Testing
ASPNETCORE_URLS=http://+:8080
DETAILED_ERRORS=true

# Application URLs
BASE_URL=http://localhost:5651
API_BASE_URL=http://api:8080/
API_URL=http://api:8080

# Authentication Configuration
JWT_SECRET=CI-Test-Secret-Key-For-Playwright-Tests-Only
JWT_ISSUER=WitchCityRope
JWT_AUDIENCE=WitchCityRopeUsers
JWT_EXPIRES_IN_MINUTES=60

# Test Configuration
CI=true
PLAYWRIGHT_TEST=true
TEST_TIMEOUT=30000
TEST_PARALLEL_WORKERS=1

# Browser Configuration
HEADLESS=true
PLAYWRIGHT_BROWSERS_PATH=0

# Logging Configuration
Serilog__MinimumLevel__Default=Information
Serilog__MinimumLevel__Override__Microsoft=Warning
Serilog__MinimumLevel__Override__System=Warning
Logging__LogLevel__Default=Information
Logging__LogLevel__Microsoft.AspNetCore=Warning

# Email Configuration (Mock for testing)
EMAIL_PROVIDER=Mock
EMAIL_FROM_EMAIL=test@witchcityrope.com
EMAIL_FROM_NAME=WitchCityRope Test

# PayPal Configuration (Sandbox/Mock)
PAYPAL_MODE=sandbox
PAYPAL_CLIENT_ID=test_client_id
PAYPAL_CLIENT_SECRET=test_client_secret

# Session Configuration
SESSION_IDLE_TIMEOUT=20
SESSION_COOKIE_NAME=.WitchCityRope.Session
SESSION_COOKIE_HTTP_ONLY=true
SESSION_COOKIE_IS_ESSENTIAL=true

# Performance and Resource Limits
DOCKER_MEMORY_LIMIT=2g
DOCKER_CPU_LIMIT=1.0